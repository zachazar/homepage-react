import * as React from 'react'
import cx from 'classnames'

import * as styles from './styles.module.scss'

const LoadingIcon = () => <div className={cx('icon is-small', styles.loader)} />

export default LoadingIcon
